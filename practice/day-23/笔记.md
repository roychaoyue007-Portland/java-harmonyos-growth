# Day 23 练习笔记：文件读写与程序输入输出

## 1. 今日主题

文件读写与程序输入输出：程序如何从文件、终端和网络获得数据。

## 2. 今日核心问题

程序运行时的数据从哪里来，又会被输出到哪里？

## 3. 今日理解

输入是进入程序的数据，输出是程序处理后的结果。

文件输入是程序从文件读取数据，终端输入是用户在终端里直接输入数据，网络输入是程序从其他设备或服务收到数据。

终端输出通常只是展示，文件输出是把结果保存到硬盘上的文件里。错误输出也是输出的一种，但它表示程序遇到了问题，通常提供排错线索。

## 4. 动手命令

```powershell
Get-Location
New-Item -ItemType Directory -Force .\practice\day-23
Set-Content .\practice\day-23\input.txt "hello day 23"
Get-Content .\practice\day-23\input.txt
Write-Output "this is terminal output"
Write-Output "this is file output" | Out-File .\practice\day-23\output.txt
Get-Content .\practice\day-23\output.txt
Add-Content .\practice\day-23\output.txt "second line"
Get-Content .\practice\day-23\output.txt
Get-Content .\practice\day-23\output.txt | Select-Object -First 1
Get-Content .\practice\day-23\output.txt | Measure-Object -Line
Get-Content .\practice\day-23\not-exist.txt
```

## 5. 观察记录模板

```text
我执行了哪些命令：
哪些命令读取文件：
哪些命令写入文件：
哪些结果显示在终端：
哪些结果保存进文件：
哪条命令产生了错误信息：
我怎样理解“输入来源”和“输出去向”：
```

## 6. 今日重点

1. `Get-Content` 读取文件内容。
2. `Set-Content` 覆盖写入文件。
3. `Add-Content` 追加写入文件。
4. `Write-Output` 默认输出到终端或管道。
5. `Out-File` 把输出写入文件。
6. 管道 `|` 把上一条命令的输出交给下一条命令。
7. 文件存在，不等于程序已经读取文件。
8. 终端显示，不等于文件保存。

## 7. 今日复盘

Day23 的重点是把 Day10 学过的输入、输出、管道、重定向，接到 Day22 的硬盘、内存、进程模型上。程序运行时会从文件、终端或网络获得输入，处理后再输出到终端、文件或其他位置。
